import React from 'react';

export default function FilterBox(props) {
  return (
    <div class="box">
      <h2>Event Type</h2>
      <h2 class="category">
        {" >> "} event type name <br />
        {" >> "} event type name <br />
        {" >> "} event type name <br />
        {" >> "} event type name<br />
        {" >> "} event type name<br />
        {" >> "} event type name<br />
      </h2>
    </div>
  )
}